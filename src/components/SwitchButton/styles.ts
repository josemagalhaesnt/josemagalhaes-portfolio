import styled from 'styled-components';

const SwitchButtonStyled = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;

    .Icon {
        padding: 0 0.5rem;
    }
`;

export default SwitchButtonStyled;
